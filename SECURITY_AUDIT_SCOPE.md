# 安全審查範圍與驗收基準

審查對象為 `CantoCandidate.exe` 及其附帶的本機詞庫檔案。目標是找出能由一般使用者、同一台電腦上的其他帳戶或惡意檔案利用的風險；本審查不聲稱取代具 Windows 實機、代碼簽署、惡意程式掃描與專業滲透測試的獨立第三方安全評估。

| 範圍 | 驗收基準 |
|---|---|
| 網絡連線 | 僅能以 HTTPS 連到固定的 `inputtools.google.com`；不放寬憑證驗證、不下載或執行遠端內容。 |
| 輸入資料 | 程式只在已啟用中文模式時處理組字；組字長度有上限，候選請求須降低連續輸入造成的資源壓力。 |
| 本機檔案 | `history.tsv` 與 `custom_dictionary.tsv` 限制在程式資料夾、UTF-8、1 MiB 上限，並拒絕目錄和重新解析點；詞庫內容只作文字資料，絕不作命令或程式碼執行。 |
| 外部程序 | 編輯詞庫時須以 Windows 系統目錄中的 Notepad 啟動，避免依賴目前目錄或 PATH 搜尋。 |
| 鍵盤掛鈎 | 不產生日誌、不把按鍵傳送到程式作者或其他服務；只有使用者主動開啟的中文模式才會將拼音組合送往候選服務。 |
| 執行檔硬化 | 產物維持 x64 GUI、自足式 C++ 執行庫，並啟用 ASLR、DEP/NX 和高熵位址配置相容旗標。 |
| 套件完整性 | 每次交付都提供 SHA-256、壓縮檔完整性檢查及完整原始碼。 |
