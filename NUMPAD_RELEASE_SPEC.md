# NumPad 候選選取規格：v0.8.1 Online 與 v0.9.1 Offline

## 分支與候選來源

v0.8.1 必須由 `v0.8` 標籤建立，保留原有線上 Google 粵語候選服務、候選快取、本機近音及語境排序。v0.9.1 必須由 v0.9 Offline Core 建立，保留 Rime-Cantonese 的 `offline_lexicon.bin`、無 WinHTTP 相依及完全離線候選。兩者只共用候選視窗與按鍵操作改善，不混合候選後端。

## NumPad 選字行為

當候選窗有候選時，`VK_NUMPAD1` 至 `VK_NUMPAD9` 必須與主鍵盤 `1` 至 `9` 完全一致：選取**目前頁面**第 1 至第 9 個候選。若該頁的候選少於相應數字，按鍵仍被攔截但不貼上任何字。沒有組字或候選時，NumPad 鍵不得被工具攔截，應交回原本程式。

主鍵盤數字與 NumPad 選字都只在有候選窗時生效；粵拼組字期間按數字不會插入聲調，因為本工具仍以不輸入聲調數字為主。NumPad Enter 在 Windows 通常映射為 `VK_RETURN`，現有 Space／Enter 選取目前焦點的行為已同時涵蓋主 Enter 與 NumPad Enter，毋須另加私有快捷鍵。

## 驗收

| 測試 | 預期結果 |
|---|---|
| 有候選時按主鍵盤 `1`–`9` | 維持既有的目前頁候選選取。 |
| 有候選時按 NumPad `1`–`9`（Num Lock 開啟） | 選取同頁對應序號候選，候選窗關閉並貼上文字。 |
| 有候選時按 NumPad 數字大於本頁候選數 | 不貼上文字、不令程式崩潰。 |
| 無候選時按 NumPad 數字 | 工具不攔截，由目標程式正常處理。 |
| 有候選時按 NumPad Enter | 選取目前焦點，與主 Enter 一致。 |
| 兩條版本候選來源 | v0.8.1 仍連線查詢；v0.9.1 仍不匯入／呼叫 WinHTTP。 |

> 兩個版本都必須保留「主鍵盤及 NumPad 只在候選可見時作選字」的原則，避免干擾一般數字輸入、遊戲或工作程式。
