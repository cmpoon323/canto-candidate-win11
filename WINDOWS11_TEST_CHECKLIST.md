# Windows 11 實機驗收清單（v0.6）

請以一般使用者帳戶執行 `CantoCandidate.exe`。不需、亦不應以系統管理員身分啟動，除非要特別驗證同權限程式的相容性。

| 類別 | 測試步驟 | 預期結果 | 結果 |
|---|---|---|---|
| 離線詞庫載入 | 確認 `offline_lexicon.bin` 與 EXE 同一資料夾；雙擊 EXE，按 `Ctrl+Alt+G` | 系統匣出現圖示，顯示「中　離線粵語拼音」。 | ☐ |
| 基本離線候選 | 斷開網絡後，在記事本輸入 `neihou`、`m4goi`、`hoenggong`、`sikfaan` | 顯示「你好」、「唔該」、「香港」、「食飯」等候選；按 `1` 或空白鍵貼上。 | ☐ |
| 詞庫缺失 | 暫時把 `offline_lexicon.bin` 改名後重啟，再開啟工具 | 顯示離線詞庫無法載入，不崩潰、不嘗試上網。 | ☐ |
| 本機近音 | 斷網後輸入 `leihou` 或 `nihou` | 精確碼無候選時，出現「本機近音」及相近候選；關閉設定後不使用近音。 | ☐ |
| 翻頁 | 輸入有較多候選的拼音，按 `PageDown`／`.` | 候選窗更新頁碼；按 `PageUp`／`,` 可返回。 | ☐ |
| Shift | 單按 Shift 兩次 | 中→EN→中；按住 Shift 再輸入英文字母時不切換。 | ☐ |
| 中文標點 | 在沒有組字時按 `.`、`,`、`?`、`!` | 依次輸出 `。`、`，`、`？`、`！`。 | ☐ |
| caret 位置 | 在記事本、Chrome 文字框、Word 中輸入拼音 | 候選窗盡量顯示在文字游標附近。 | ☐ |
| caret 後備 | 在無法讀取 caret 的 App 或將 `follow_caret=0` | 候選窗退回至滑鼠位置，程式不崩潰。 | ☐ |
| 剪貼簿保留 | 先複製一段文字 A，再選字輸入「你好」，150 ms 後貼上 | 剪貼簿仍是 A。 | ☐ |
| 剪貼簿競態 | 選字後 150 ms 內立即複製文字 B | 剪貼簿保留 B，不會被還原成 A。 | ☐ |
| 設定檔 | 將 `single_shift_toggle=0` 後重啟 | 單按 Shift 不再切換；右擊系統匣可仍可切換。 | ☐ |
| 短語展開 | 輸入 `;thanks` 後按 `Space`／`Enter` | 直接貼上 `唔該晒！`，不建立網絡候選請求。 | ☐ |
| 短語失敗 | 輸入不存在的 `;unknown` 後按 `Enter` | 顯示找不到短語提示，不會貼上或崩潰。 | ☐ |
| 近音關閉 | 將 `local_fuzzy_suggestions=0` 後重啟，再以相同方式離線輸入 `leihou` | 不使用本機近音候選。 | ☐ |
| 語境排序 | 依次確認「你好」→ 下一輪輸入某有多個候選的拼音並選目標詞；重複後再建立相同前詞 | 已學習的相鄰候選只在相同前詞下獲較高排序；候選仍可手動選擇。 | ☐ |
| 語境清除 | 系統匣選「清除語境歷史」後重新測試 | `context_history.tsv` 被清除，排序回復原狀。 | ☐ |
| 語境關閉 | 將 `context_ranking=0` 後重啟 | 不再建立或使用新的相鄰詞排序。 | ☐ |
| 外觀設定 | 將 `dark_mode=0`、字體設為 `28`、每頁設為 `5`，重啟程式 | 候選窗變淺色、字體較大、每頁最多顯示 5 個候選。 | ☐ |
| 資料清除 | 系統匣選「清除所有本機資料」並確認 | 短語、詞庫、歷史、語境和舊快取被清除；短語及詞庫範例重新建立。 | ☐ |
| 斷網 | 關閉網絡後輸入已覆蓋及未覆蓋的拼音 | 覆蓋拼音正常出候選；未覆蓋拼音顯示 `OFFLINE：找不到候選字`，不嘗試連線。 | ☐ |
| 多螢幕／DPI | 在不同螢幕、150% 或以上縮放使用 | 候選窗不超出工作區且可讀。 | ☐ |
| 權限邊界 | 嘗試在以管理員身分開啟的記事本輸入 | 一般權限工具不應跨權限貼字；不嘗試提升或繞過。 | ☐ |

如有失敗項目，請記錄 Windows 11 版本、目標程式及版本、是否使用多螢幕／高 DPI、輸入步驟和截圖，方便後續定位。
