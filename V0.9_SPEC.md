# CantoCandidate v0.9 Offline Core 規格

## 目標

v0.9 必須在 Windows 11 x64 上解壓即用，中文候選查詢的整個過程不建立任何 HTTP／HTTPS 連線。程式沿用現有候選視窗、貼上、短語、歷史、語境與設定功能，但候選來源改為同資料夾的本機索引檔 `offline_lexicon.bin`。

## 詞庫範圍與授權

首版只編譯 Rime-Cantonese 中具「詞語 + 粵拼」直接映射的 `jyut6ping3.words.dict.yaml` 與 `jyut6ping3.chars.dict.yaml`。未標音的 `jyut6ping3.phrase.dict.yaml` 不會直接納入，避免以多音字猜測粵拼而產生不可靠候選。任何發行檔均須包含 `THIRD_PARTY_NOTICES.md`、`LICENSE-CC-BY`、`LICENSE-ODbL`，標示 Rime-Cantonese、來源連結、CC BY 4.0 與 maps 的 ODbL 1.0、以及資料經過編譯與正規化。

## 索引格式

編譯器讀取 YAML-like 詞條，將詞語、標準聲調粵拼及無聲調正規化碼寫入自訂二進位索引。索引按正規化碼排序，並包括 UTF-8 詞語、顯示粵拼與固定來源序；候選查詢以二分搜尋取得同碼字詞，再以自訂詞、歷史、語境和穩定原始次序排序。索引不包含網絡端點、遙測或外部依賴。

## 容錯與輸入

程式接受聲調數字、空格與撇號；正常候選以聲調移除後的正規化碼查詢。若沒有精確候選，才套用最多 8 個明示的本機變體：`n/l`、首音 `ng` 有／無、`h/f`、末尾 `ng/n`、單字元刪除與相鄰字母交換。近音候選必須標示「本機近音：匹配碼」。

## 驗收標準

| 類別 | v0.9 驗收要求 |
|---|---|
| 網絡 | `CantoCandidate.exe` 不再匯入或呼叫 WinHTTP；斷網下可完成候選查詢。 |
| 詞庫 | 編譯器可重複產生相同版本的索引；報告輸入詞條、去重數、索引大小及 SHA-256。 |
| 延遲 | 對固定測試集的 100 次查詢量測平均與 P95 本機查詢時間。 |
| 正確性 | 測試集中的常用粵拼可取得至少一個合理候選；測試需分別列出單字、詞語、聲調、省略與近音。 |
| 私隱 | 程式碼、相依與導入表不得包含候選服務 URL；本機資料仍可由系統匣清除。 |
| 授權 | 發行包包含第三方通知、完整授權副本和資料處理說明。 |

> v0.9 是離線候選 MVP，不承諾與 Google 的雲端排序、最新詞彙或長詞語預測等同。其產品承諾是：可攜、無網絡候選、可解釋與可清除的本機資料。
