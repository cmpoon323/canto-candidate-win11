# CantoCandidate v0.7：日常效率版規格

v0.7 保持 Windows 11 x64 免安裝定位。所有新資料預設只寫在 EXE 同一資料夾；不建立帳戶、不雲端同步、不上傳歷史、快取或短語內容。只有用戶在中文模式輸入而需要新候選時，拼音組合才會發往既有 Google 候選服務。

| 功能 | 本機檔案 | 行為與邊界 |
|---|---|---|
| 常用短語 | `snippets.tsv` | 使用 `;` 作觸發前綴，例如 `;addr`。按空白鍵／Enter 直接展開，不呼叫網絡候選服務。格式：`shortcut<TAB>text`。 |
| 離線候選快取 | `candidate_cache.tsv` | 每次 Google 查詢成功後保存拼音及最多 45 項候選。網絡失敗或無候選時才使用快取；上限 2,000 組或 1 MiB，按最近使用淘汰。 |
| 本機資料管理 | 系統匣選單 | 可開啟短語、快取、歷史、詞庫、設定；可清除候選快取或所有本機資料。資料管理仍由使用者使用 Windows Notepad 編輯，以避免在可攜版引入額外 UI 框架。 |
| 操作設定 | `settings.ini` | 加入 `candidate_font_size`（16–32）、`candidate_page_size`（5–9）、`dark_mode`（0/1）及 `snippet_prefix`（預設 `;`）。無效值回退安全預設。 |

> 短語與候選快取可能含使用者自訂文字或拼音，屬個人資料。預設放於 EXE 資料夾，方便手指碟攜帶，但也代表該資料夾的其他使用者可讀取。使用者可用系統匣選單清除資料，或直接刪除相應 TSV 檔案。
