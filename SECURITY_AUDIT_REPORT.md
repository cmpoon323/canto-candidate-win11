# CantoCandidate v0.5 安全審查報告

審查日期：2026-08-15（GMT+8）。審查依據為可重複的靜態來源碼檢查、交叉編譯產物的 PE 匯入／安全旗標檢查、網絡端點驗證及壓縮包完整性驗證。此結果不是獨立第三方滲透測試，也不取代在 Windows 11 實機執行的防毒掃描與端點防護檢查。

## 結論

在此輪檢查中，**沒有發現可確認的遠端命令執行、未加密網絡傳輸、下載後執行、後門資料上傳或任意檔案路徑寫入漏洞**。我發現並修正了四項防護不足：未設上限的遠端回應、可被異常本機檔案型態影響的詞庫讀寫、以搜尋路徑啟動記事本，以及高速連續輸入會產生過多候選請求。v0.5 已針對這些問題加入明確限制。

| 類別 | 結果 | 證據與處理 |
|---|---|---|
| 網絡傳輸 | 通過 | 程式只以 `WINHTTP_FLAG_SECURE` 連到固定主機 `inputtools.google.com` 的 HTTPS 預設連接埠；來源碼沒有憑證略過、TLS 降級或代理覆寫。 |
| 遠端回應大小 | **已修正** | 所有候選服務回應現限制為 **1 MiB**；超出即停止讀取並回報錯誤，避免惡意或異常回應耗盡記憶體。 |
| 輸入與候選請求 | **已修正** | 拼音組字最多 **64 字元**；候選請求加入 **120 ms** 合併延遲，降低快速按鍵造成的執行緒／網絡壓力。 |
| 本機詞庫檔案 | **已修正** | `history.tsv` 與 `custom_dictionary.tsv` 的讀寫均限制 1 MiB，拒絕目錄和重新解析點，並以 `FILE_FLAG_OPEN_REPARSE_POINT` 開啟；詞庫內容只作文字候選，不會當作命令或程式碼執行。 |
| 外部程式啟動 | **已修正** | 開啟詞庫時改用 `%WINDIR%\\System32\\notepad.exe`，不再依賴 PATH 或目前資料夾搜尋 `notepad.exe`。 |
| 鍵盤資料 | 通過但屬敏感功能 | 程式使用低階鍵盤掛鈎，但只在使用者以 `Ctrl+Alt+G` 開啟工具且為中文模式時攔截拼音；沒有鍵盤日誌、檔案記錄或額外上傳端點。 |
| 剪貼簿 | 已知行為 | 選字時會暫時把選定中文詞放進 Windows 剪貼簿並送出一次 `Ctrl+V`。這是可攜式跨程式輸入所需行為，亦代表會覆蓋你原有剪貼簿內容。 |
| 動態載入／進程注入 | 未見程式碼使用 | 來源碼沒有 `LoadLibrary`、`GetProcAddress`、`CreateProcess`、`WinExec`、遠端執行緒、記憶體寫入或下載檔案 API。PE 產物出現 `LoadLibraryW`／`GetProcAddress` 是 MinGW 靜態執行庫的內部匯入，不存在於本專案來源碼，且沒有用戶可控 DLL 名稱。 |
| Windows 產物硬化 | 通過 | PE `DllCharacteristics` 包含 `HIGH_ENTROPY_VA`、`DYNAMIC_BASE`（ASLR）與 `NX_COMPAT`（DEP/NX）。 |
| 非系統 DLL 依賴 | 通過 | 只匯入 `GDI32`、`KERNEL32`、`MSVCRT`、`SHELL32`、`USER32`、`WINHTTP`。 |
| 任意下載／自動更新 | 未見 | 來源碼沒有 URL 下載、更新器、外掛載入或遠端執行邏輯。 |

## 剩餘風險與建議

| 風險 | 等級 | 原因與建議 |
|---|---|---|
| 未簽署的可攜式 EXE | 中 | 任何人若在你下載或解壓後替換 `CantoCandidate.exe`，Windows 無法以數位簽章辨識來源。請只由可信來源取得、核對 SHA-256、把程式放在你自己有寫入權限的資料夾，避免使用公共可寫的 USB 或共享資料夾。 |
| 拼音會送往 Google | 中（私隱） | 已啟用中文模式時，拼音組合會傳到 Google 以取得候選。不要在密碼、銀行、個人識別資料等敏感欄位啟用。 |
| 剪貼簿被覆蓋 | 低 | 選字後原有剪貼簿內容會被候選詞取代；如要保留內容，先貼上或複製到其他位置。 |
| 高權限程式 | 低 | 本工具以一般權限執行時，無法貼字到管理員權限程式，這是 Windows 安全隔離；不應為此以管理員權限常駐執行。 |
| Windows 實機行為 | 低至中 | 本輪建置環境是 Linux，無法在此執行低階鍵盤掛鈎和 Windows Defender 實時掃描。請在 Windows 11 實機測試並使用 Defender 掃描壓縮檔／EXE。 |

> **安全使用建議：** 核對本交付檔的 SHA-256 後解壓，在一般權限帳戶內使用；只於需要輸入粵語時啟用中文模式；不要把程式、詞庫或歷史檔放在不可信人士可寫入的資料夾。

## 重現檢查

以下是本次審查已執行的核心檢查：來源碼掃描網絡、檔案、外部程序、鍵盤掛鈎和進程注入相關 API；PE 匯入表檢查；PE 安全旗標檢查；編譯；壓縮檔 `unzip -t` 檢查；以及 SHA-256 建檔。完整審查範圍見 `SECURITY_AUDIT_SCOPE.md`。
