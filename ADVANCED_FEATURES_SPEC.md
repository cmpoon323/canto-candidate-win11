# 進階輸入功能規格（v0.3）

本版本延續「解壓即用、非原生 IME」的原則，並加入候選翻頁、本機選字歷史、自訂詞庫、Shift 中英切換及較接近 Google Input Tools 的候選選取節奏。Google 的候選服務只在啟用中文模式及鍵入拼音時收到拼音組合；個人化功能完全在程式同一資料夾內的檔案處理，不會上傳至 Google 或任何第三方。

| 功能 | 使用方式 | 資料處理與限制 |
|---|---|---|
| 候選翻頁 | `PageUp`／`PageDown`、`↑`／`↓`、`,`／`.`、`-`／`=` 在候選頁之間移動 | 每頁最多 9 個候選；服務回傳的候選不足 10 個時，沒有更多頁。 |
| 候選焦點 | `←`／`→` 移動焦點，`Space`／`Enter` 選目前焦點；`1–9` 可直接選該頁候選 | 取消組字或換頁時，焦點回到該頁第一個候選。 |
| Shift 中英切換 | 單獨按一下 `Shift` 切換「中」或「EN」；按住 Shift 再配合其他按鍵則維持一般 Shift 行為 | 英文模式不攔截字母，文字直接輸入原程式；`Ctrl+Alt+G` 照常總開關。 |
| 本機選字歷史 | 每次選字後，該拼音與詞語的本機使用次數增加；下次相同拼音時常用詞優先 | 記錄在 `history.tsv`，包含拼音、詞語、使用次數及最近使用時間；不會傳送。可在系統匣選單清除。 |
| 本機自訂詞庫 | 編輯 `custom_dictionary.tsv`，每行為 `拼音<TAB>詞語` | 自訂詞會置頂但保留 Google 候選，其後再依歷史排序。可由系統匣選單開啟；程式每次查詢重新載入。 |
| 候選視窗 | 顯示「中／EN」、原始拼音、頁碼、容錯讀音與帶焦點的候選 | 介面語意與快捷鍵接近 Google Input Tools，但不複製 Google 的商標、程式碼或完整介面。 |

> **私隱注意：** 本機歷史和自訂詞庫是使用者明確要求的個人化功能，因而會儲存部分拼音和所選詞語。若不想保留，請在系統匣選單按「清除本機歷史」，或刪除工具資料夾內的 `history.tsv`。自訂詞庫可隨時刪除或修改。

## 中文標點輸入（v0.4）

當工具為「中」模式、而且**沒有正在組字的拼音**時，常用標點會由工具直接輸出全形中文標點：`.`→`。`、`,`→`，`、`?`→`？`、`!`→`！`、`:`→`：`、`;`→`；`、`(`→`（`、`)`→`）`、`[`→`「`、`]`→`」`、`'`→`、`、`"`→`「`、`/`→`／`、`\\`→`、`。英文模式下，所有標點都維持鍵盤原本輸出。

為避免衝突，當用戶正在組字且候選已經出現時，`,`、`.`、`-`、`=` 繼續代表前後翻頁，並不輸出標點。若已組字但尚未收到候選，這些標點會先作為全形標點輸出，並取消未完成組字；這避免網絡延遲時無法輸入句號或逗號。
