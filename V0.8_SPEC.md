# CantoCandidate v0.8：透明模糊配對與本機語境排序

v0.8 不會宣稱複製 Google 的未公開排序邏輯。程式保留 Google 候選服務的原始排序，並只在原始查詢無候選或服務失敗時，以本機規則尋找**已存在的離線候選快取**；所有近音結果會標示「本機近音建議」。

| 功能 | 資料與規則 | 私隱與控制 |
|---|---|---|
| 本機近音建議 | 對目前拼音產生最多 8 個單一修正變體：`n↔l`、開首 `ng` 的有／無、`h↔f`、末尾 `ng↔n`、單一字母刪除或交換。僅從 `candidate_cache.tsv` 找已快取候選，不新增網絡請求。 | `local_fuzzy_suggestions=1` 可關閉；候選窗會顯示實際採用的本機近音拼音。 |
| 本機語境排序 | 每次選字後，將上一次上屏詞與本次詞的相鄰關係記於 `context_history.tsv`。同一拼音的 Google 原候選中，只有曾在目前前詞後出現的候選會獲小幅加分。 | `context_ranking=1` 可關閉；檔案只在本機保存，最多 5,000 條關係，系統匣可獨立清除。 |
| 排序原則 | 自訂詞永遠最優先；若無明確本機語境關係，維持既有歷史排序及 Google 原先次序。 | 不記錄完整句子、按鍵原文或視窗內容；只保存用戶**已確認上屏**的相鄰候選詞。 |

> 語境排序只係本機「下一個詞傾向」功能，不會把已上屏詞、短語、歷史或自訂詞傳送到候選服務。近音建議只用既有快取，故首次離線輸入陌生錯拼不會神奇產生候選。
