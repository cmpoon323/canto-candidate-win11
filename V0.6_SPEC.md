# CantoCandidate v0.6 規格

v0.6 聚焦免安裝版本的可靠性與可調整性，不將主線改成需要註冊的 Windows 原生 IME。候選字服務、容錯候選、全形標點和本機個人化維持原有行為。

| 項目 | 預設行為 | 後備／限制 |
|---|---|---|
| 候選窗位置 | 讀取前景視窗 UI 執行緒的 caret rectangle，並將候選窗顯示於文字游標下方。 | 無法讀取 caret 的程式、受保護欄位或跨權限視窗會退回至滑鼠位置。 |
| 剪貼簿保留 | 選字前經 OLE 取得目前完整 Windows 剪貼簿資料物件；貼上後 150 ms 如剪貼簿未被使用者改動，還原原始內容。 | 使用者在 150 ms 內自行複製內容時不還原，避免覆蓋新剪貼簿；OLE 無法取得資料時仍照常貼字但不保留。 |
| 設定檔 | 與 EXE 同資料夾的 `settings.ini`，首次啟動自動建立。 | 只接受值 `0` 或 `1`；不認識、無效或過大的設定檔會忽略並採用安全預設值。 |
| 可設定項目 | `follow_caret`、`preserve_clipboard`、`single_shift_toggle`。 | 設定檔可由系統匣選單以系統 Notepad 開啟；重開程式才讀取新值。 |

> v0.6 不承諾在每個 App 都能取得 caret。免安裝的低階鍵盤掛鈎程式不等同 Windows TSF/IME；如遇到高權限、密碼、遊戲或受保護視窗，應自然退回或不輸入，絕不嘗試繞過系統保護。
