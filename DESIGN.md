# 可攜式粵語候選字工具：設計規格

本工具的目標是在 **Windows 11** 提供近似免安裝輸入法的體驗。程式會以單一可攜式執行檔運作，毋須安裝程式、毋須把自己註冊為 Windows 語言列 IME，亦不會寫入啟動項或持久化設定。

## 運作方式

程式由使用者以 `Ctrl + Alt + G` 主動開關。只有在開啟後，程式才攔截英文字母、退格鍵、空白鍵、Enter、Esc 及數字鍵 1–9。字母會組成一段近似英文拼音，透過 HTTPS 傳送到 Google Input Tools 的粵語候選字服務；程式會顯示最多九個候選字詞。按數字鍵、空白鍵或 Enter 選取候選後，程式將選定文字放入剪貼簿並模擬 `Ctrl + V` 貼到原本正在使用的視窗。

| 元件 | 做法 | 目的 |
|---|---|---|
| 程式形態 | .NET 8 自足式 Windows x64 執行檔 | 不要求目標電腦另行安裝 .NET 或輸入法套件 |
| 按鍵處理 | 僅在使用者啟用後的低階鍵盤掛鈎 | 提供跨程式打字體驗；停用後完全不處理鍵盤內容 |
| 候選服務 | Google Input Tools 的 HTTPS 候選字請求，代碼 `yue-hant-t-i0-und` | 沿用 Google 粵語候選字引擎 |
| 選字介面 | 不取得焦點的最上層候選字視窗 | 顯示候選而不令目標 App 失焦 |
| 輸出文字 | Windows 剪貼簿及一次 `Ctrl + V` | 避免以鍵碼逐字輸入帶來字元集問題 |

## 私隱與限制

工具不會寫入日誌、資料庫或網絡分析資料，亦不會在未啟用時擷取按鍵。使用者在啟用期間輸入的拼音組合會傳送給 Google 以取得候選字，因此不應在密碼欄、網上銀行或其他敏感欄位啟用。這是以 Google Input Tools 服務製作的非官方工具，並非 Google 發布、認證或支援的 Windows IME。候選服務需網絡連線；無網絡時程式會保留已輸入拼音並提示無法取得候選。

本工具屬「可攜式常駐程式」而非 Windows 原生 TSF/IME。它能做到解壓即用和跨多數一般桌面程式輸入，但受系統安全邊界限制：若目標程式以系統管理員權限執行，而本工具不是以相同權限執行，Windows 會阻止貼上；密碼欄或部分受保護程式也可能拒絕貼上。程式不會嘗試繞過這些限制。

## 操作規則

| 操作 | 功能 |
|---|---|
| `Ctrl + Alt + G` | 開啟或關閉粵語輸入模式 |
| `a`–`z` | 輸入近似英文拼音，例如 `neihou` |
| `Backspace` | 刪除一個英文字母 |
| `1`–`9` | 選取相應候選字詞 |
| `Space` 或 `Enter` | 選取第一個候選字詞 |
| `Esc` | 取消本段拼音，保留輸入模式 |


## 容錯候選策略（v0.2）

Google 候選服務本身已會回傳模糊配對結果及其讀音標註。因此更新版不會用本機字典覆蓋或重排 Google 的候選；它會完整保留服務所回傳的前九項候選。程式只讀取第一候選的 `annotation`，把空格移除後與使用者輸入作不分大小寫比較。兩者不相同時，候選視窗將加上「容錯讀音：`…`」提示，例如輸入 `leihou` 時顯示「容錯讀音：nei hou」。

這個做法讓使用者清楚知道系統已容錯，但不會在無法確定意圖時自動替換文字。候選選取仍須由使用者按空白鍵、Enter 或數字鍵確認。程式不保存任何錯拼、選字或讀音資料；`annotation` 只在記憶體中用於目前一段輸入的提示，清除組字後即會釋放。
