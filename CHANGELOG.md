# 版本紀錄

本專案由可攜式粵語候選字輔助工具逐步演進。v0.5 是首個可重現建置並正式公開發佈的安全修正版；v0.1 至 v0.4 的原始中間二進位檔沒有保留，因此以下紀錄描述功能演進，而不把事後重建的檔案誤標為歷史原始產物。

## v0.5 — 安全修正版

加入安全審查和防護修正：HTTPS 固定候選服務端點、1 MiB 遠端回應上限、64 字元組字上限、120 ms 查詢合併延遲、詞庫檔案大小與重新解析點防護、固定使用 Windows 系統 Notepad，以及 ASLR、DEP/NX 和高熵位址配置相容旗標。隨附完整安全審查報告、範圍文件、發行 ZIP 和 SHA-256。

## v0.4 — 中文標點

在中文模式加入常用全形標點，例如 `.`→`。`、`,`→`，`、`?`→`？`、`!`→`！`，並讓候選顯示時的 `,`、`.`、`-`、`=` 保留作翻頁鍵。

## v0.3 — 進階候選與本機個人化

加入最多 45 個候選、每頁 9 個的翻頁、焦點選字、單按 Shift 中英切換、本機選字歷史排序和可編輯 UTF-8 自訂詞庫。

## v0.2 — 容錯讀音提示

保留 Google 粵語候選服務的近音／錯拼配對，並在候選讀音與原始拼音不同時顯示容錯提示。

## v0.1 — 初始可攜版

建立 Windows x64 免安裝背景候選工具：`Ctrl+Alt+G` 開關、近似粵語拼音、Google 候選字、數字選字與剪貼簿貼上。
