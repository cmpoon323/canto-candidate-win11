# 版本紀錄

本專案由可攜式粵語候選字輔助工具逐步演進。v0.5 是首個可重現建置並正式公開發佈的安全修正版；v0.1 至 v0.4 的原始中間二進位檔沒有保留，因此以下紀錄描述功能演進，而不把事後重建的檔案誤標為歷史原始產物。

## v0.8 — 透明本機近音與語境排序

服務離線或原始查詢無候選時，可由 `candidate_cache.tsv` 以最多 8 個本機近音變體尋找後備候選，並清楚標示匹配拼音，不產生額外網絡請求。加入可關閉的 `context_history.tsv`，僅紀錄已確認相鄰候選詞並對同一前詞後的候選作小幅排序；系統匣可開啟或清除語境歷史。

## v0.7 — 日常效率版

加入 `;shortcut` 常用短語展開、最多 2,000 組的本機候選快取與服務離線後備。系統匣新增短語、詞庫、歷史、快取及設定的管理入口，並可清除本機資料。`settings.ini` 新增深淺色候選窗、字體大小、每頁候選數和短語前綴設定。

## v0.6 — 可靠性與可調整性

候選窗預設跟隨前景文字游標，無法讀取 caret 時會安全退回至滑鼠位置。選字時以 OLE 備份剪貼簿，150 ms 後只在使用者未改動剪貼簿時還原。新增 `settings.ini`，可調整 caret 跟隨、剪貼簿保留和單按 Shift 中英切換；加入 Windows 11 實機驗收清單與 GitHub Actions Windows 建置流程。

## v0.5 — 安全修正版

加入安全審查和防護修正：HTTPS 固定候選服務端點、1 MiB 遠端回應上限、64 字元組字上限、120 ms 查詢合併延遲、詞庫檔案大小與重新解析點防護、固定使用 Windows 系統 Notepad，以及 ASLR、DEP/NX 和高熵位址配置相容旗標。隨附完整安全審查報告、範圍文件、發行 ZIP 和 SHA-256。

## v0.4 — 中文標點

在中文模式加入常用全形標點，例如 `.`→`。`、`,`→`，`、`?`→`？`、`!`→`！`，並讓候選顯示時的 `,`、`.`、`-`、`=` 保留作翻頁鍵。

## v0.3 — 進階候選與本機個人化

加入最多 45 個候選、每頁 9 個的翻頁、焦點選字、單按 Shift 中英切換、本機選字歷史排序和可編輯 UTF-8 自訂詞庫。

## v0.2 — 容錯讀音提示

保留 Google 粵語候選服務的近音／錯拼配對，並在候選讀音與原始拼音不同時顯示容錯提示。

## v0.1 — 初始可攜版

建立 Windows x64 免安裝背景候選工具：`Ctrl+Alt+G` 開關、近似粵語拼音、Google 候選字、數字選字與剪貼簿貼上。
